---
description: Follow AGENTS.md as the single source of truth for workflow and quality gates
alwaysApply: true
---

# ControlPlane agents

- Obey root `AGENTS.md` for workflow, TDD, and merge rules.
- Cut short-lived feature branches from `master`; squash-merge back to `master` (no durable `macOS-*` branches).
- Use project subagents/skills under `.cursor/agents/` and `.cursor/skills/` for planner, implementer, UI review, verify, and code review.
